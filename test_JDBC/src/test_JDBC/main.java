package test_JDBC;

public class main {
	
	public static void main(String[] args) {
		
        /*
         * 
         * Què no entenc?
         * 
         * Alhora d'afegir la llibreria/driver, a on s'ha d'afegir exactament?
         * S'ha de crear un projecte normal? O un web project
         * Afegir al NETBEANS:
         * https://www.tutorialsfield.com/how-to-connect-mysql-database-in-java-using-netbeans/
         * 
         * INSTALAR XAMPP I CONECTOR JDBC 
         
         CREATE DATABASE IF NOT EXISTS nom_usuari;
		 CREATE USER 'nom_usuari'@'localhost' IDENTIFIED BY 'nom_usuari';
		 GRANT ALL PRIVILEGES ON nom_usuari.* TO 'nom_usuari'@'localhost';
		 FLUSH PRIVILEGES;
		 added

         
         
        -- Creació de la taula 'countries'
        CREATE TABLE countries (
            id INT AUTO_INCREMENT PRIMARY KEY,  
            nom VARCHAR(100) NOT NULL,
            capital VARCHAR(100) NOT NULL
        );
        
        INSERT INTO countries (nom, capital) VALUES
            ('Espanya', 'Madrid'),
            ('França', 'París'),
            ('Itàlia', 'Roma'),
            ('Alemanya', 'Berlín'),
            ('Portugal', 'Lisboa');
        */
	}
	

}
